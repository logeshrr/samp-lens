{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
      "deploy": "vite build && gh-pages -d dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
     "@snap/camera-kit": "latest"
  },
  "dependencies": {
    "@snap/camera-kit": "^1.3.0"
  }
}
